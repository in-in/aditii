header
    .hdr-top
        .container
            .row
                .l-logo.logo Aditii
                .l-cart.cart
                    svg.icon.icon-cart
                        use(xlink:href="#icon-cart")
                    .price $300
                .l-search.search
                    .dropdown
                        .dropdown-title Menu
                            .caret
                        +list(menuItem)(role="dropdown").menu
                    input(type="text", name="search").search-input
    nav
        .container
            +list(menuItem)(role="horizontal").row.menu
    // BANNER
    include banner
    // BANNER ends
    // slider
    include slider
    // slider ends